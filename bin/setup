#!/bin/bash

set -e

bin=$(dirname $0)
mkdir -p /usr/libexec/kubernetes/kubelet-plugins/volume/exec/kubernetes.io~ubiquity/

cp $bin/../out/spectrum /usr/libexec/kubernetes/kubelet-plugins/volume/exec/kubernetes.io~ubiquity/ubiquity
