apiVersion: v1
kind: PersistentVolume
metadata:
  name: ibm-ubiquity-db
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 3Gi
  hostPath:
    path: /tmp
  persistentVolumeReclaimPolicy: Delete
